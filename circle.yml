machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 7.9.0

dependencies:
  override:
    - yarn --pure-lockfile
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - yarn test

